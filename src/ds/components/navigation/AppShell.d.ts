import * as React from 'react';

/**
 * Dark gradient page shell with the max-w-5xl centered column. Wrap every
 * screen in one; children are cards spaced 24px apart.
 * @startingPoint section="Navigation" subtitle="Dark shell and content column" viewport="700x300"
 */
export interface AppShellProps extends React.HTMLAttributes<HTMLDivElement> {}
export declare function AppShell(props: AppShellProps): JSX.Element;
