# Deploy-time function params (firebase-functions defineString).
# Display From for outgoing mail — keep it matching the SMTP account, or an
# alias that account may send as, or Gmail will rewrite it.
MAIL_FROM=OronoHR <noreply-hr@orono.k12.mn.us>
